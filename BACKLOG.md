# Backlog

Durable register of considered proposals that were deferred, scoped out, or
rejected. Read before drafting a new SPEC.md; swept at turn close.


### board-negative-control (first revisit criterion FIRED 2026-07-17; still open) (ACTIVE in spec 2026-08-03)
- **The review board's false-positive rate is unmeasured: it has been calibrated on one org, one model, one run, with no negative control** (`docs/REVIEW-CALIBRATION.md`, and the README's "What this does not prove" section says so publicly). Nothing establishes how often `/forge-review` manufactures a finding against prose that is fine.
- **Status 2026-07-17:** the first revisit criterion below has fired and the entry did not close. M11b engaged this entry by boarding `northgate-staffing` -- a corpus whose ground truth no reader had seen -- but boarding is not measuring: the run produced 16 majors and no negative control, so the FP rate is exactly as unknown as before. The mitigation held instead: every finding carried into the README was re-verified against a ledger by hand. The tally of *reviewer* false positives caught this way is now five (`CODEREVIEW.md` keeps it running); the most recent, 2026-07-17, was an exploration reporting `BACKLOG.md`'s `$500,500` as a miscount when it is correct. Critics and reviewers both need the same scepticism, and the case for a real negative control gets stronger every time the tally moves.
- **Why deferred:** out of scope for M8 (fabric history and date-scoped briefs). Judged lower-urgency because every one of the M7 board's 11 majors against fernhollow cites ledger-traceable evidence and is checkable by hand without the board: `rf:finance-1`'s claim that every expense line is a fixed share of revenue is confirmed in 30 seconds by reading `fabric/finance.py:12-18,48` (hardcoded `_EXPENSE_CATEGORIES` weights, split out of an `expense_total` that is itself `year_revenue * expense_ratio`). A negative control matters when findings are not self-verifying.
- **Revisit criteria:** the board is pointed at a corpus whose ground truth the reader has not already read (a new fixture, an outside org, or the M10 reference fleet); or a board finding drives expensive work and cannot be confirmed against a ledger; or `/forge-fix` is proposed, since a fix loop turns the critic into a gate and its FP rate becomes load-bearing.
- **Origin:** spec 2026-07-16 (M7 proposal, "The board has no negative control").
- **Annotated 2026-07-28 (M17):** no boards ran this turn, and the FP rate is unchanged. What did land is a mechanical alternative to the board for one finding class: the exemplar's `Jim` collision, which four of six reviewers caught, is now derived deterministically into `evals/diagnostics.json` as an unplanned-alias sighting and can be rejected outright at ingest by `graph_targets.alias_agreement`. That is one finding class moved from critic to oracle. It does not measure the critic's FP rate on everything else, and the entry stays open.


### concurrent-workers-share-one-scratchpad
- **Concurrently dispatched workers share one session scratchpad and can clobber each other's scratch files.** Affects both fan-out paths. Found first in MODEL-AB Round 2, where four board reviewers ran at once and one read another's extracted text, then asserted a checkable falsehood about the *other* arm (claiming byte-identical prose across arms; all 22 docs differed). Then observed on the live `/forge` path during the M11b fleet run: two concurrent forge-author workers overwrote each other's validation scripts, and one reported the other's batch failures under its own command. No bad artifact resulted either time -- the airlock accepted every batch, and per-arm review verdicts stayed sound because each reviewer read its own share -- but a worker that distrusts its own checks burns a turn, and a reviewer that reads a sibling's evidence can publish a false finding.
- **Why deferred:** `/forge` now tells each worker to namespace its scratch by work order (Step 3b), which held for the remaining four orgs of the fleet run, so the live path has a working mitigation. The real fix is harness-level per-worker scratch isolation, which OrgSmith does not control: the skill can only ask. `/forge-review` is per-slug over one org, so its six reviewers read the same share by design and have nothing to cross-contaminate; the reviewer half only bites a board dispatched across two or more orgs at once, which no committed skill does.
- **Scale note added 2026-07-17, and M12's spec must take a position on it.** The mitigation is prompt-level: the skill asks each worker to namespace its scratch, and a worker that ignores the ask fails silently. That held across 38 batches at M11b. The flagship is **~334 batches** (`docs/SCALE.md`), roughly nine times the exposure, and the failure is probabilistic rather than deterministic -- "held for four orgs" is not a rate. `docs/EXTERNAL-CRITIQUE-2026-07-17.md` makes the same point from outside and is right about it: "Prompting workers to namespace scratch files is a mitigation, not isolation." Neither observed instance produced a bad artifact (the airlock accepted every batch), so the honest framing is that this costs turns rather than correctness -- but that claim rests on the airlock catching what the scratchpad confuses, and it should be stated deliberately rather than assumed at nine times the batch count.
- **Revisit criteria:** the harness gains per-subagent scratch isolation (delete this then, and drop the prompt guidance from `/forge` Step 3b); or a board is dispatched over more than one org in a single session; or the prompt-level mitigation is observed failing -- a worker citing evidence that cannot be located in its own assigned batch; or M12 is specced, which multiplies the exposure by ~9 and is the first run large enough to turn a low per-batch probability into a likely event.
- **Origin:** spec 2026-07-17 (M11b; reviewer half found by MODEL-AB Round 2's harness, author half found live during the fleet run).
- **Annotated 2026-07-28 (M17):** the turn's regeneration half plans ~45 authoring batches across three orgs plus three boards, roughly the M11b exposure again rather than the flagship's nine-fold. The mitigation is unchanged and still prompt-level (`/forge` Step 3b asks each worker to namespace its scratch by work order), and the airlock remains the correctness backstop: a confused worker costs a turn, not an artifact. Restated here so the exposure is on the record before the batches run.

### generator-fingerprinting
- **A consumer can learn OrgSmith's tells rather than enterprise capability, and nothing here tests for it.** Raised by `docs/EXTERNAL-CRITIQUE-2026-07-17.md` and absent from this repo in any form. The candidate fingerprints are all real: a stable `Engagements/Finance/Firm/People` taxonomy, a filename grammar regular enough to parse with a regex (`YYYY.MM.DD - Genre - Client - vN FINAL.ext`), nine genre rows in one registry, one placeholder-to-surface rendering path, published location-hard-case policies, one OCR corruption profile, a uniform synthetic-provenance marker on every file, and prose from a single model family. A retrieval system that learns "status reports live two folders down and end in v2 FINAL" scores well here and has learned nothing about a real share.
- **The value of the entry is the reframe, not the news.** The evidence is already published: `cross-document-voice` records that every genre collapses to one template. We file that as a realism defect. It is *also* a benchmark-validity threat, and the second framing is the one that bites a consumer, because a fingerprint does not have to look unrealistic to be learnable. The same fact is a different problem depending on who is reading.
- **Status 2026-07-22 (M15): two named fingerprints eroded, and the entry stays open because erosion is not a defense.** (1) The filename grammar is no longer uniform where noise lives: `noise.filename_variety` decorates derived filenames from a variant grammar (`Copy of X`, `X (1)`, `X_old`, `X FINAL FINAL`, `X EXECUTED DO NOT USE`), and `ashcombe-advisory` ships five distinct patterns, so a regex tuned to `YYYY.MM.DD - Genre - Client - vN FINAL.ext` now misses part of the share. (2) The voice-template fingerprint is eroded rather than removed: `doc_culture.style_specs` gives each person a structured spec (register, sentence-length bias, greeting/closing forms, habits, banned tics) that reaches the brief per author. Neither is a defense in this entry's sense. The variant grammar is *itself* a fixed grammar drawn from a published pool in `render/noise.py`, learnable in exactly the same way and now shipped in the repo a consumer reads; the style pools in `foundation/style.py` are equally enumerable, and the spec is a brief-level nudge whose effect nothing here measures (`cross-document-voice` stays open on that point). The stable taxonomy, the single genre registry, the uniform provenance marker, and the single model family are all untouched. Recorded so the next reader does not mistake "the filenames vary now" for progress against the threat model this entry is actually about.
- **Why deferred:** the defenses are cheap individually and meaningless individually. Held-out recipes, hidden seeds, an unpublished flagship variant, adversarially altered directory and filename conventions, multiple authoring models, and (the critique's own preference) an independently implemented second generator only mean something against a stated threat model and a consumer who is actually scoring. Nobody is scoring yet. Building hold-outs before there is a claim to protect is scaffolding.
- **In tension with a published position, which is why this is a decision and not a task.** The README's whole strategy is that everything is browsable, checkable, and committed -- "nothing to clone, install, or authenticate". Held-out seeds and hidden recipes are the opposite of that by construction. M12 is scoped as a capability showpiece rather than a benchmark, so the tension does not bind yet; it binds the moment someone reports a score.
- **Revisit criteria:** any external party reports scores against a committed org (the entry becomes urgent, not merely open); or OrgSmith is submitted or cited as a benchmark rather than a generator; or a second authoring model is used for a committed org, which is the cheapest fingerprint defense and is already half-built (`docs/MODEL-AB.md`); or the flagship's recipe is considered for withholding.
- **Origin:** `docs/EXTERNAL-CRITIQUE-2026-07-17.md` (external critique, the one finding in it that was genuinely new).

### external-validity-program
- **Nothing establishes that a system scoring well on OrgSmith scores well on a real corpus, and the repo says so** (README, "What this does not prove"). `docs/EXTERNAL-CRITIQUE-2026-07-17.md` names the distinction precisely: **verification** (did the software implement its design correctly?) versus **validation** (is the design an adequate model of organizations?). OrgSmith is strong on the first -- 445 tests, a byte pin, 29 validator rules -- and has nothing on the second. The critique's proposed remedy is an "M11.5 validation milestone" producing a Flagship Validity Contract: declared use cases with claims and non-claims, measured target distributions (files per employee-year, emails per employee-day, thread-depth and response-time distributions, sender/recipient degree, document-type proportions, temporal burstiness, version-cluster sizes, graph motifs, missingness and error rates), a benchmark split strategy, a pre-registered transfer protocol, and downstream validation against at least one real corpus and ideally an independently constructed second generator.
- **Why deferred:** it is a research program, not a milestone, and it presumes a positioning decision that has been made the other way. **M12 is scoped as a capability showpiece, not a peer-reviewed benchmark** (user decision, 2026-07-17). Target distributions require a reference population OrgSmith does not have and cannot legally acquire for most of the listed statistics; transfer experiments require a real corpus, a scoring harness, and several systems to run; an independent second generator is a second project. Adopting the frame would also mean claiming a scientific standard the project would then have to hold, which is worse than not claiming it.
- **What was adopted instead, so this entry is not read as a rejection:** the parts that are cheap and stand alone. `emit-evals` already ships scored suites for four of the critique's seven declared use cases (retrieval, extraction, entity resolution with alias credit and per-ambiguity-class recall, ACL-aware visibility), derived deterministically from ledgers; M12 takes the nested-splits idea (core / +distractors / +noise / full, one eval suite across all four) so the flagship produces a retrieval degradation curve rather than a headline number; and the noise model gives the denominator the critique correctly identifies as where real retrieval difficulty comes from.
- **The honest position it does license, and the README already takes:** develop and diagnose on OrgSmith, evaluate transfer on a real corpus, report the gap. Without the third step a good OrgSmith score is an internal result. That is a caveat the README states rather than a program the repo runs.
- **Revisit criteria:** OrgSmith is submitted, cited, or adopted as a benchmark rather than a generator (the non-claims stop being sufficient the moment someone else makes the claim); or an external party reports scores and asks what they transfer to; or a real organizational corpus with usable aggregate statistics becomes available under terms that permit publishing a comparison; or a collaborator arrives who wants the validation half as their contribution.
- **Origin:** `docs/EXTERNAL-CRITIQUE-2026-07-17.md`; scoped out the same day.
- **Annotated 2026-07-28 (M17):** the cheap slice is adopted. Keyless retrieval baselines (`docs/BASELINES.md`, filename-only and BM25 per org per split) and per-org data cards (`DATA-CARD.md`, feature matrix, question counts, split gaps, residuals, non-claims) give a consumer a floor and a characterization without claiming transfer. The transfer program itself stays out for the reasons above, and every data card states the non-claim explicitly. A second external critique (2026-07-28) raised the same theme and got the same answer.


### event-simulation
- **Documents are generated from a plan, not emitted by a simulated organization.** `docs/EXTERNAL-CRITIQUE-2026-07-17.md`: "A real organization is a process generating artifacts, not a collection of individually plausible artifacts... Do not generate more documents. Generate a simulated organization whose activities naturally emit documents." The process-level tells it lists are real and mostly already logged here: engagement activity disappears for years while hiring continues, meetings land on weekends, prose contradicts the ledger's reporting lines, every document is deliberate. A discrete-event model (hiring, projects, billing, meetings, approvals, travel, deadlines, disputes, leave, reporting changes) would emit documents as observations of events rather than as registry rows.
- **Why deferred:** it is a rewrite of `fabric` and `docplan`, the two stages every committed org's ledgers derive from, and it would break the byte pin on all seven. It is also further along than the critique knew. **M9 already moved one step in exactly this direction**: `docplan/registry.py` replaced a fixed `2E + 7` genre skeleton with driver-derived supply -- each genre names a DRIVER (`per_engagement`, `per_fiscal_year`, `firm_periodic`, `per_hire`) and a CADENCE, so "document count falls out of the firm's real activity -- its engagements, fiscal years, and hires". That is events-emit-documents for four coarse event classes. The gap is that the drivers are structural facts rather than a simulated timeline: nothing decides that *this* meeting happened on *this* day because *that* deadline moved.
- **The cheaper intermediate is what M12 actually takes.** Most of the critique's process complaints have a knob-shaped fix that does not need a simulator: a business-day calendar constrains the genres that assert attendance; deriving revenue from the engagement book closes the fee/revenue gap; a noise model supplies the abandoned and superseded content. Each lands additively and leaves the fleet pinned. A simulator is what you build when the knobs stop composing, and they have not yet.
- **M14 (2026-07-21) advances process realism the same knob-shaped way, not by rewriting the fabric.** Email threads now emit as observations of an engagement's activity (minute-granularity timing in business hours, reply chains, transmittals carrying the kickoff), and a mundane internal genre populates the mailbox with non-engagement traffic. That is documents-as-observations for the mail surface, still driven by the existing drivers rather than a simulated timeline. The signal to revisit remains the knob-composition test below: this is one more knob that composes, so the entry stays open.
- **Revisit criteria:** two or more process defects arrive that cannot be expressed as independent knobs (the signal that the knobs have stopped composing); or a recipe needs causal dependency between events (a slipped deadline moving a meeting moving a status report); or `fabric` is rewritten for any other reason; or a flagship-scale board finds process incoherence the knobs were supposed to fix.
- **Origin:** `docs/EXTERNAL-CRITIQUE-2026-07-17.md` ("Realism currently stops at the document boundary").


### state-json-mixes-execution-and-provenance
- **`state.json` is simultaneously mutable run state and a published, committed artifact.** It carries per-stage completion, per-doc `authored_hash`/`rendered_hash`/`rendered_from`, outstanding work-order coordination (`outstanding`, `author_batches`), generator provenance (`generators`), and capability probes -- and it is committed with all seven orgs. `docs/EXTERNAL-CRITIQUE-2026-07-17.md` argues these want opposite semantics (execution wants mutation, provenance wants immutability and audit) and proposes a three-way split: run state, build manifest, artifact index.
- **Why deferred:** the tension is real in principle and has not bitten in practice, and the fix costs the one property the file exists to provide. CLAUDE.md's hard rule is "Resume state is file-derived, never conversation memory", and `state.json` is the single source of resume truth; splitting it creates three files that can disagree with each other, which is the failure mode `SPEC.md` (M11b) explicitly refused when it rejected a fleet-level progress ledger: "a new ledger would be state that can disagree with the files". The file is also deliberately timestamp-free so regeneration stays byte-stable, which is most of what the "immutability" half of the critique wants. Splitting it would also move or break `orgsmith/state@1` for every committed org.
- **The critique's stated trigger is worth recording because it is testable:** it expects this to matter "when M12 runs are large, distributed, resumed across machines, or generated with multiple workers". Three of those four already happened at M11b -- 38 batches, windows 9 wide, a cross-session resume after an ~80-minute idle with 9 orders outstanding, all nine re-dispatched and ingested with zero duplicated or lost documents (`docs/SCALE.md`). Distributed across machines is the one that has not, and nothing plans it.
- **Revisit criteria:** generation is ever distributed across machines or filesystems (the case the current design genuinely does not cover); or a resume bug is traced to the file carrying two kinds of state; or `state.json` needs a schema bump for another reason, since the split is cheapest to do at the same time; or an external consumer needs build provenance without the run state attached to it.
- **Origin:** `docs/EXTERNAL-CRITIQUE-2026-07-17.md` ("state.json mixes execution state and published provenance").


### recipe-coherence-test-has-no-floor
- **`test_fleet_recipe_growth_headcount_and_span_describe_one_firm` is one-sided: it asserts `margins[-1] < _NET_MARGIN_CEILING` (0.40) and has no lower bound** (`tests/test_org_regen.py:112,252`). It catches the incoherence it was built for -- a firm compounding fees against a frozen roster, posting a margin no professional-services firm posts -- and nothing else. A recipe whose `base_revenue` is far too low for its headcount posts a deeply negative margin and passes, because a negative number is less than 0.40.
- **Found 2026-07-17 by trying to publish the opposite.** A README sentence claimed the coherence test would reject the absurd-low-revenue tuning that would close the fee/revenue gap. It would not. The claim was checked before the commit landed and the README now says the true thing, including that the test misses this.
- **Why deferred:** no recipe is anywhere near the floor, so this is a gap in what the test *could* catch rather than a bug it is missing today (measured 2026-07-17: every fleet org posts a healthy positive margin, and the entry that motivated the test, `dev-mini-margin-incoherent`, is a *ceiling* case at 43.1%). Adding a floor means choosing a number, and unlike the ceiling -- where "no professional-services firm posts a 50% net margin" is a defensible anchor -- the floor has no obvious one: real firms do post losses, especially young ones, and the founding year is already excluded as a deliberate 0.45x ramp. A badly-chosen floor would fail an honest recipe.
- **Revisit criteria:** any recipe is written whose intended margin is negative or near zero (the floor stops being hypothetical and the number can be chosen against a real case); or the fee/revenue coupling from `engagement-ledger-reads-as-whole-book` lands, since deriving `base_revenue` from the engagement book makes low-revenue recipes reachable by accident rather than by typo; or the test is touched for any other reason.
- **Origin:** spec 2026-07-17 (found while verifying a README claim about the test, which was false).


### mail-audience-internal-vs-external (fixture half CLOSED by M16, 2026-07-28)
- **Closed 2026-07-28 (M16), fixture half:** `ashcombe-advisory` was regenerated under recipient-aware reply guidance, and its M16 board's document_plausibility pass did not recur the internal-note-delivered-to-the-client finding (its email findings are the full-name-in-body device and thread-state regression instead). The capability-level question — a mixed internal/external mail-thread design — remains open. The historical finding below is the record of the closed fixture defect.
- **Engagement-email replies are all delivered To the external client (the deterministic To/Cc partition puts external participants in To, internal in Cc), but the model sometimes authors them as internal staff-to-staff notes.** The M14 `ashcombe-advisory` pilot board flagged it independently on three dimensions (narrative_consistency blocker, graph_acl_naturalness blocker, document_plausibility): `d:0064` (Suarez-Jones) renders `To: Jennifer Herrera` (client) but its body reads "before it goes anywhere near the client, so we are not sending Jennifer Herrera anything we have not both read"; same in `d:0049`, `d:0079`, `d:0006`. The reply brief guidance (`authoring/contexts.py`) tells the author it is a mid-thread reply but never names the recipient or distinguishes a client-facing thread from an internal one, so the model guesses the audience.
- **Why deferred:** out of scope for M14, whose job was landing the thread capability and proving it in a fixture (done; the pilot validates clean and scores 100%). The fix is a realism refinement — brief guidance that names the recipient contact, or a mixed internal/external thread design where some engagement mail is genuinely internal — not a prose edit (the pilot's prose is frozen; the board finding is published, per the house rule).
- **Revisit criteria:** M15 (voice/brief derivation touches the reply guidance anyway); or the M16 pilot regeneration (re-author under recipient-aware briefs, closing the finding with a fixture); or a mixed internal/external mail-thread design is specced.
- **Origin:** spec 2026-07-21 (M14 pilot board, `ashcombe-advisory`).
- **Annotated 2026-07-28 (M17):** the recipient-mention exemption knob lands this turn, which removes the mechanical reason an email author has to spell the recipient's full legal name in the body. That closes the device, not the design question: mixed internal/external threads, where some engagement mail is genuinely staff-to-staff, are still unmodelled and the entry stays open on that.

### noise-kinds-deliberately-excluded
- **Four noise kinds a real share has and OrgSmith deliberately will not plant: corrupted or unopenable files, personal material, contradictory corrections, and broken cross-document links.** User-accepted exclusions at M15's adoption, recorded here so the absence reads as a decision rather than an oversight. M15 landed six kinds (duplicates, drafts, diverging version chains, misfiles, dead templates, junk directories) plus filename variety and attachment-version mismatch; these four were considered against the same criteria and refused.
- **Corrupted / unopenable files.** FILE-01 is a tamper oracle: it opens every rendered file and fails the org if one will not parse. That rule is how a stripped or edited distributed corpus is caught. Planting deliberately unopenable files would mean teaching FILE-01 which corruption is sanctioned, which is exactly the shape of exception that makes a tamper check stop meaning anything. The realism gain (real shares contain truncated downloads and zero-byte files) does not pay for weakening the strongest oracle in the repo.
- **Personal material.** Real shares carry lunch menus, someone's resume, holiday photos, a mistakenly saved bank statement. Generating plausible personal documents about synthetic people is a different product with a different risk surface, and the shape most worth having (material that looks like it leaked in by accident) is the shape most likely to be quoted out of context as something OrgSmith produces about people. Declined on judgment, not cost.
- **Contradictory corrections.** A document that states a fee, followed by a correction email that states a different one, is the sharpest retrieval hard case a corpus can carry. It also breaks the load-bearing invariant: all numbers come from the ledgers, and the ledger holds one value per fact. Supporting it means the ledger owning fact *histories* with validity intervals, every eval answer becoming as-of-a-date, and PROV/extraction attribution rewritten around them. That is a schema turn, not a noise kind, and it should be specced as one rather than smuggled in under `noise:`.
- **Broken cross-document links.** Dead references (a memo citing a report nobody can find) are common and cheap to plant. They collide directly with MAN-01's 1:1 manifest-to-share contract and with the mention machinery, which exists to guarantee that every planned surface resolves. A deliberately unresolvable reference is indistinguishable, to every existing rule, from a generation bug. Making it distinguishable means a second class of "intentionally dangling" reference in the manifest, which is real design work for the least valuable of the four.
- **Why deferred:** each is refused for its own reason above; none is refused for cost alone. The common thread is that three of the four require weakening or extending an oracle (FILE-01, the ledger's single-value facts, MAN-01), and this repo's whole argument is that the oracles are the part you can trust.
- **Revisit criteria:** a consumer reports that a retrieval or extraction system passes here and fails on real shares specifically because nothing is corrupt, contradictory, or dangling (that turns a judgment call into a measured gap); or fact histories with validity intervals are specced for another reason, at which point contradictory corrections become nearly free; or an eval suite is added that scores as-of-a-date answers; or the personal-material exclusion is revisited under an explicit policy about synthetic material concerning synthetic people.
- **Origin:** spec 2026-07-22 (M15; user-accepted exclusions at adoption, logged by the final criterion).

### mundane-broadcast-names-a-recipient-in-the-body
- **A distribution-list broadcast greets one person by first name and then names them in full to the room**, because `mail.exempt_recipient_mentions` deliberately does not apply to DL mail: the `To` header carries the list address rather than member names, so MENT-01 still needs the body mention. Found by the M17 board on the regenerated exemplar (`d:0009`, `d:0051`, flagged independently by document_plausibility and narrative_consistency).
- **Why deferred:** the exemption landed and closed the device on person-to-person mail, which was the fleet-wide artifact M17 set out to fix. Closing the DL half needs either a mention model that understands list membership or a planner that stops giving a broadcast a single participant, and either is a capability change plus a regeneration.
- **Revisit criteria:** a mail org is regenerated for any other reason; or the mention model gains list-membership awareness; or a consumer reports the broadcast prose as unrealistic.
- **Origin:** spec 2026-07-29 (M17 board, `northgate-staffing`).

### exemplar-has-no-2020
- **The exemplar's finance ledger has no COVID**: revenue rises 10% with no soft quarter across 2015-2023, and its two largest travel years are 2020 and 2021, in a corpus whose own 2021 overview says more first meetings now happen over video. Found by the M17 board (finance_realism).
- **Why deferred:** the finance model is a smooth growth curve by construction, and adding era shocks is the `event-simulation` entry's territory rather than a knob. Faking one firm's 2020 by hand would be a prose fix to a ledger-shaped problem.
- **Revisit criteria:** `event-simulation` is specced; or a recipe needs a declared era shock (a named year with a revenue and travel multiplier); or a board finds the same flatness on a second org, which would make it a fleet property rather than one recipe's.
- **Origin:** spec 2026-07-29 (M17 board, `northgate-staffing`).

### cross-document-voice (ACTIVE in spec 2026-08-03)
- **Every genre collapses to one template across authors, years, and clients.** The M11b board's largest cluster, and still present in the M17-regenerated exemplar: two kickoff memos two years apart by two authors are the same memo re-skinned (`rf:voice-1`, blocker), six engagement-opening emails run one script across five authors (`rf:voice-2`), and a single rhetorical move recurs in fourteen documents across eight authors and five genres, paraphrased each time (`rf:voice-3`). The board's own `rf:voice-7` is the diagnosis: per-person voice genuinely works, and what recurs is the per-genre OUTLINE.
- **Any single number for it is a claim about taste wearing a decimal point.** The board reported "34 occurrences" for the antithesis tic; measured over the corpus, strict readings land at 3, 5 and 11 depending on the pattern, a looser one gives 20, and the plain words give 146 across 43 of 44 documents. Nothing adjudicates, because no ledger owns "is this the same rhetorical figure". The README cites the range and the one count reproducible without being told the pattern.
- **THE MEASUREMENT HALF IS NOW PARTLY CLOSED (M17b).** `orgsmith/review/structure.py` scores same-genre pairs on block skeleton and on positional openers, neither of which a paraphrase moves. Calibrated against the board's own findings in `docs/REVIEW-CALIBRATION.md` (2026-08-02): it ranks the `rf:voice-1` blocker 5th of 161 pairs where the lexical metric does not flag it, `rf:voice-5` 2nd, and five of `rf:voice-6`'s fifteen pairs in the top ten. It **misses** `rf:voice-4` entirely at rank 78, which both proxies miss, and it cannot reach `rf:voice-3` at all -- a paraphrased sentence-level move across five genres is not a pair, not a shape, and not an n-gram. Reported beside the lexical score, never gating.
- **THE CAUSE HALF HAS A CANDIDATE FIX, UNPROVEN ON PROSE (M17b).** `doc_culture.outline_variety` deals each authored document a section skeleton from a per-genre pool, no variant repeating within any k consecutive documents of a genre, and authoring ingest enforces what a skeleton forbids. What is proven today is plumbing and block-shape counts under a scripted author. **No committed org has adopted the knob and no fixture was regenerated**, so whether real prose stops converging is unmeasured.
- **Why still open:** the fix is landed but unexercised. The cause remains a fresh-context worker with no view of its siblings' prose, which is also what makes parallel authoring possible (M10) and what keeps the airlock honest. `rf:voice-3` is out of reach of any keyless proxy and of the outline work both, and needs semantics.
- **Revisit criteria:** the M18 flagship (or any regeneration) is generated with `outline_variety` on and a board reads the result -- that is the only thing that settles whether the fix works; or the structural axis is re-calibrated against a second org; or a reader cites the tic without being pointed at it.
- **Origin:** spec 2026-07-17 (M11b board, five majors); measurement half addressed and re-scoped at spec 2026-08-02 (M17b).

### packaging-and-archival
- **What remains of the external critique's packaging ask, with the done parts removed.** As of v2.2.0 the package IS installable: `pyproject.toml` carries `[build-system]`, `[project.scripts]` and `[project.dependencies]`, a `Dockerfile` pins the generation environment (Pango for WeasyPrint, LibreOffice for legacy conversion), `requirements.lock` records exact versions per release, and `schemas/` publishes every inter-stage contract as JSON Schema. The entry's original text claimed none of that existed and was factually stale by two milestones; this is the rewrite.
- **What is genuinely still missing:** hash-locked dependencies (the lock file carries version pins, not `--hash` entries, so transitive deps float and a compromised index is not detected); DOI-backed archival storage; and a machine-readable CLI specification (`docs/EXTERNAL-CRITIQUE-2026-07-17.md` line 408). `CHECKSUMS.md` covers the corpus but nothing outside this repo pins it.
- **Why deferred:** the remainder is release-and-archival work, not milestone work. DOI-backed archival in particular is a claim about permanence that should follow the corpus being final, and the fleet is still being regenerated turn to turn.
- **Revisit criteria:** the corpus is cited or published somewhere needing a stable identifier; or someone outside this box consumes the generator through an API rather than the CLI, which is when a machine-readable CLI spec starts paying; or a supply-chain requirement makes version pins insufficient.
- **Origin:** `docs/EXTERNAL-CRITIQUE-2026-07-17.md` ("Packaging is too informal"); rewritten at spec 2026-08-02 (M17b) after the original text went stale.
