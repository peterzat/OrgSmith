# Runtime dependencies for the pipeline (charter -> assemble, validate).
# Pinned exactly; update deliberately, one at a time.

pydantic==2.13.4        # inter-stage contracts in orgsmith/schemas.py
Jinja2==3.1.6           # HTML templates for WeasyPrint PDFs
PyYAML==6.0.3           # ORG-CHARTER.md recipe block parsing
Faker==40.31.0          # seeded deterministic names/addresses in foundation scaffold
python-docx==1.2.0      # .docx renderer
XlsxWriter==3.2.9       # .xlsx renderer (write_formula with cached values)
weasyprint==69.0        # primary PDF renderer (needs system Pango)
pypdf==6.14.2           # PDF text extraction for the fact-echo validator
pikepdf==10.10.0        # PDF provenance metadata
openpyxl==3.1.5         # xlsx reader; runtime because the validator reads cached cell values
python-pptx==1.0.2      # .pptx renderer and validation-time reader
pypdfium2==5.11.0       # pdf rasterization for the scan pipeline (generation)
Pillow==12.3.0          # scan degradation (rotation, noise, contrast)
numpy==2.2.6            # seeded noise fields for scan degradation
olefile==0.47           # pure-python OLE reading: legacy container + provenance checks
xlrd==2.0.2             # .xls reader so FIN-02 ties legacy workbooks to the ledger
